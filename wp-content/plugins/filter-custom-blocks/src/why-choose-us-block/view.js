/**
 * WordPress dependencies
 */
import { domReady } from '@wordpress/dom-ready';

/**
 * Frontend JavaScript for the Why Choose Us block
 */
domReady(function() {
    // Initialize any frontend-specific functionality or interactivity if needed.
    // This file is loaded on the frontend when the block is used.
    
    // For now, this is a minimal implementation as no special interactivity is required
    // for this static block.
    
    // If adding interactivity in the future, select the blocks properly:
    // const blocks = document.querySelectorAll('.wp-block-block-development-examples-why-choose-us-block');
    
    // blocks.forEach(function(block) {
    //     // Add your interactive functionality here
    // });
}); 